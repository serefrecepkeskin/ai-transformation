# Mimari

> **PLACEHOLDER** — bunu gerçek kod tabanından doldurmak için
> `bootstrap-research` skill'ini çalıştır. Kısa tut; bir mimari karar
> değiştiğinde bu dosyayı güncelle ve aynı PR'da bir ADR yaz.

## Render stratejisi

TODO(confirm): SSR / CSR / hibrit — hangi sayfa hangisini kullanıyor ve neden.

## Klasör yapısı

```
TODO: klasör başına tek satır notla gerçek üst seviye ağaç
```

## Katmanlar

- **UI bileşenleri** — TODO: primitive'ler nerede yaşıyor, ne içerebilir/içeremez
- **Feature bileşenleri** — TODO: domain kompozisyonları, veri erişim deseni
- **State** — TODO: client-state kütüphanesi; neyin store'a, neyin URL'e,
  neyin yerel state'e ait olduğu
- **Veri çekme / API katmanı** — TODO: uygulama backend'lerle nasıl konuşuyor,
  doğrulama nerede yapılıyor, hata kontratı ne

## Dağıtım & sürüm

TODO(confirm): dal modeli, CI kapıları, bir merge nasıl sürüme/dağıtıma dönüşüyor.
