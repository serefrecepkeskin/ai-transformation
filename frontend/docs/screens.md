# Screen inventory

> **PLACEHOLDER.** Filled by the bootstrap prompt from the router and the menu
> definition; kept in sync with them afterwards. `ui-check` walks this table
> screen by screen, role by role; an empty table makes it toothless.

Sources: TODO(confirm) — the router file(s) and the menu/navigation source.
Roles: TODO(confirm) — the personas a tester can log in as (no credentials here).

| Screen | Route | Gate (permission / role) | Roles to check | Must show / must not show |
|---|---|---|---|---|
| TODO | `/` | — | any | TODO |

Conventions: one row per screen a user can reach; tabs and modals with their
own state get their own row; "must show" names the empty, error and gated
states that have a designed appearance.
