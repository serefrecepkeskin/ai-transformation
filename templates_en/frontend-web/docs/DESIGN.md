---
# PLACEHOLDER — machine-readable design tokens, written by `/impeccable document`
# (scan mode extracts them from the real CSS/Tailwind/theme). Tokens are
# normative: do not hand-edit them into disagreement with the code.
name: PROJECT_NAME
description: PLACEHOLDER — one-line tagline
colors: {}
typography: {}
rounded: {}
spacing: {}
components: {}
---

# Design System

> **TEMPLATE.** This file is produced and refreshed by the `impeccable` skill
> (`/impeccable document`), not written by hand. It follows the DESIGN.md
> format spec: token frontmatter above, then the eight sections below in this
> fixed order — keep the headings verbatim so DESIGN.md-aware tooling can read
> them. It lives in `docs/` (not the repo root) so the knowledge base stays in
> one place; impeccable resolves `docs/` natively. See
> [ADR 0002](decisions/0002-adopt-impeccable-design-quality.md).

## Overview

PLACEHOLDER: the design philosophy in a few sentences — the atmosphere the
product aims for, and what makes a screen feel like it belongs to this product.

## Colors

PLACEHOLDER: what each color in the frontmatter is *for* (roles, not values):
which one carries action, which one is the ground, what never touches what.

## Typography

PLACEHOLDER: the type roles (display / heading / body / caption) and when each
is used; the scale's intent, not a repeat of the token values.

## Layout

PLACEHOLDER: grid, container widths, spacing rhythm, breakpoints, and how
density changes between viewports.

## Elevation & Depth

PLACEHOLDER: shadow/overlay levels and what each level means (resting, raised,
overlay, modal). If the design is flat, say so — that is a decision.

## Shapes

PLACEHOLDER: corner radii, borders, and the shape language (soft/geometric),
including where sharp corners are deliberate.

## Components

PLACEHOLDER: the canonical components and their states. Component tokens live
in the frontmatter; this section explains when to reach for which component,
and which existing component to extend rather than reinvent (rule #4).

## Do's and Don'ts

PLACEHOLDER: the project-specific rules the detector cannot infer — the visual
moves that are always right here, and the ones that are always wrong.
